# 项目状态

更新：2026-09-02。本文只记录可验证的当前状态；产品与架构见 [README](README.md)，部署见 [部署手册](docs/部署手册.md)。

## 里程碑

| 里程碑 | 状态 | 交付范围 |
| --- | --- | --- |
| M1 生成核心回路 | 完成 | 项目、会话、LangGraph 生成、构建、SSE、预览、任务恢复与取消。 |
| M2 可控修改回路 | 完成 | 点选/聊天修改、快照、diff、接受/撤销、构建与交互校验、失败回滚。 |
| M3 素材与质量闭环 | 完成 | 异步素材任务、候选选择、四维交付评估和受控低分修复。 |
| M4 静态发布 | 完成 | 版本发布、公开 slug 地址、稳定项目地址、恢复和下线。 |
| M5 运营管理 | 完成 | 管理台、配额/账号操作、素材重试与审计。 |
| M6-0 可观测性 | 完成 | 健康、队列、成功率、告警及项目执行时间线。 |
| M6-1 命令沙箱 | 完成 | 可选 Docker 命令隔离和参数回归测试。 |
| M6-2 生产部署 | 进行中 | Compose、Nginx、PostgreSQL、备份/恢复资产已具备；需持续完成线上验收。 |
| M7 Alpha 数据集成 | 规划中 | OpenAPI/Swagger 导入、契约解析、字段绑定与真实数据验证。 |
| M8 Beta 全栈交付 | 概念阶段 | 受控后端、数据库、鉴权、测试与部署。 |

## 已交付的关键事实

- 所有者隔离的身份、项目、模板、会话和消息 API；技术栈路由会在 HTML 与 Vue 3 冲突时要求确认。
- 生成与修改共享模型契约、工具 schema、权限策略和安全展示；修改任务禁止执行命令。
- 工作台使用 SSE；事件具有递增 ID，断线可用 `Last-Event-ID` 补发，运行中任务可重新订阅。
- Vue 项目从 `dist/` 预览。真实构建会使用 `npm run build -- --base=./`，避免 `/preview/<id>/` 下资源误请求为根路径 `/assets/...`。
- 版本、素材清单和发布产物均使用 ASCII 工作区标识，不使用显示名称或项目数字 ID 作为文件路径。
- 发布只复制版本快照，稳定项目地址始终指向当前激活发布版本；不会公开工作区目录。
- 生产 Compose 将 PostgreSQL、backend、web 置于内部网络；web 仅绑定 `127.0.0.1:8080`，由宿主 Nginx 提供 HTTPS。

## 已知边界

- SSE 缓冲和任务队列仍在进程内；尚无跨进程实时恢复或 Redis 队列。
- 预览 iframe 为保证生成项目可运行，仍允许脚本、同源、表单和弹窗；更严格的预览隔离待完善。
- 视觉评估依赖 Chromium 截图与独立视觉模型；未配置时明确降级，不伪造评分。
- 外部图片来源需要各自 API Key；无可用来源时素材任务会返回原因并降级。
- `mock` 构建不产生新的 Vue `dist`，不能用于验证 Vue 预览或发布。

## 当前优先级

1. 完成线上 HTTPS、真实 API、备份与恢复演练，并验证发布、预览和 Vue 实际构建。
2. 完善预览隔离、独立命令执行器和跨进程任务队列。
3. 以 OpenAPI/Swagger 导入启动 M7，保留现有 demo 生成模式。
