# 变更记录

## 2026-08-20：Agent 评测基线与首屏优化

- 新增离线 Agent 回归样例，覆盖 HTML/Vue 路由、生成工具工作流及修改澄清/交互约束。
- 新增浏览器验收清单，覆盖技术栈确认、SSE 回放、修改结果回放和预览性能。
- 前端不再全量安装 Element Plus，字体改为 Latin 子集，降低入口 JavaScript 与全局 CSS 负担。

## 2026-08-20：提示词目录化

- 提示词收敛为路由、生成、修改 3 个 Agent 模板，以及生成工作流的解析、计划、总结 3 个子任务模板；移除未接入的构建修复提示词与一行式提示词碎片。
- 新增提示词目录索引，业务代码统一使用 `load_prompt` / `render_prompt` 加载并注入上下文；技术栈分流组件改名为路由 Agent。
- 将提示词改为契约式结构：明确角色边界、决策规则、工具工作流、质量门槛、澄清策略和机器可解析输出格式。
- 提示词结构统一使用 Markdown 标题与列表，避免在 Markdown 文件中混用 XML 块。
- 真实 DeepSeek 冒烟已覆盖 HTML 生成、路由 Agent 的三种确认结果，以及修改 Agent 新增可点击按钮。

## 2026-08-20：首次生成技术栈确认

- 新增技术栈分流接口：根据需求建议 HTML 多文件页面或 Vue 3 工程，建议与首页选择不同时才要求确认。
- 用户可切换到建议栈或明确保留原选择；生成 Agent 将已确认技术栈视为硬约束，HTML 项目写入 Vue/Vite 文件会被服务端拦截。

## 2026-08-20：首页缩略图按需生成

- 项目卡片进入视口附近才请求截图，避免首页同时为所有项目启动无头浏览器任务。
- 成功缩略图缓存 1 小时；截图的固定渲染等待从 5 秒调整为 3 秒。
- 移除截图失败后的自动强制重试，避免大型 Vue 项目持续删除缓存并重复排队；保留手动重试。

## 2026-08-20：修改原子回滚与 SSE 游标恢复

- 修改任务在编辑前保留恢复点；构建或交互校验失败时自动还原工作区，避免失败任务遗留半完成源码。
- SSE 事件增加递增 `event_id` 和最近事件缓存，支持浏览器通过 `Last-Event-ID` 在断线后补发遗漏事件。
- 新增按钮请求增加静态交互校验：源码中必须存在点击事件绑定，否则任务失败并回滚。

## 2026-08-20：修改落盘、交互与确认状态修复

- 修改 Agent 禁止编辑 `dist`、`build` 等构建产物，Vue 源码修改完成后自动重新构建预览，确保刷新和重新进入项目读取同一份结果。
- 修改 Agent 增加交互控件约束：新增按钮、开关和表单必须同时带有可用行为与反馈，不能只渲染外观。
- 接受和撤销结果写入修改记录与会话历史；重进项目后卡片会稳定还原为已接受或已撤销，不再重复要求选择。

## 2026-08-20：聊天意图分流、修改确认与阶段恢复

- 修复重新进入运行中项目时固定回退到“规划”阶段的问题，改为从同一会话已持久化的最近阶段事件恢复。
- 聊天输入新增项目上下文意图分流：点选元素和修改语义进入修改任务；明确新建/重建语义才进入生成任务。
- 修改任务在编辑前自动创建快照，完成消息携带结果版本；新增一键撤销 API 与结果确认卡片，可恢复到本次修改前的版本。

## 2026-08-20：运行任务恢复与项目隔离

- 新增项目维度的生成/修改活动任务查询接口；重新进入项目会恢复任务状态并重新订阅事件流。
- 工作台路由按项目 ID 重建，项目切换会关闭旧 SSE 与清空旧状态，避免聊天、预览和任务串用。
- 补充生成、点选修改、聊天修改、任务恢复与项目隔离的回归覆盖。

## 2026-08-20：修改任务聊天流与回放修复

- 点选修改提交后，聊天页现在订阅并展示定位、思考、模型正文、工具、文件、差异与完成事件。
- 聊天输入增加“直接修改”模式；已点选元素时精确修改，未点选时可按明确文本替换指令修改源码。
- 修改历史不再把阶段名、完整源码和 diff 作为普通助手气泡回放；文件内容只在需要时查看，差异以折叠卡片展示。

## 2026-08-20：统一 ASCII 存储命名

- 新项目工作区标识改为 `yyyy-mm-dd-hh-mm-ss-random`，与项目显示名完全解耦。
- 缩略图和版本快照改用同一工作区标识；版本快照子目录也不再使用 `v1` 或项目数字 ID。
- 服务启动时迁移旧的中文或数字工作区目录到规范路径，并兼容清理旧缩略图和版本目录。

## 2026-08-20：模型与 Agent 工具调用分层

- 新增 Provider 无关的模型请求、响应和工具调用契约；OpenAI 兼容 HTTP/SSE 协议解析移入独立 Provider。
- 新增统一 Agent 工具 schema、`ToolCall`/`ToolResult`、策略校验和执行器；生成与修改 Agent 共用该调用链。
- 修改 Agent 明确禁止命令执行；敏感路径、越权工具、无效参数与过大写入会在执行前拦截。
- 验证：`python -B -m pytest -q` 通过（70 passed）。

## 2026-08-19：生成工作区与实时预览体验

- 将生成项目工作区按技术栈隔离为 `storage/workspaces/vue/<slug>` 和 `storage/workspaces/multifile/<slug>`，并禁止使用纯数字项目名称。
- Vite 构建使用相对资源路径，平台通过 `/preview/<project_id>/` 托管 `dist` 预览，修复资源路径错误造成的白屏。
- 约束生成 Agent 不启动本地开发服务器：仅允许安装依赖和构建；拒绝 Linux 专用的超时、休眠、临时目录、重定向和后台命令。
- 生成过程使用 SSE 展示阶段、思考、工具、文件和构建日志；等待提示改为不遮挡聊天内容的轻量进度条，结束后自动隐藏。
- 预览区域区分生成中、未生成、失败和可预览状态，并修复预览 iframe 已就绪时空态仍覆盖页面的问题。

## 验证

- `backend`: `python -B -m pytest -q`，67 passed。
- `frontend`: `npm run build`，通过类型检查与 Vite 构建。
