[pytest]
# This workspace's mounted filesystem can block pytest's cache finalizer after
# all tests have passed. The cache is not needed by this test suite and can
# leave CI/developer commands hanging at 100%.
addopts = -p no:cacheprovider
testpaths = tests
