fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0.30,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
alembic>=1.13,<2.0
httpx>=0.27,<1.0
pytest>=8.2,<9.0

# 文档指定技术栈（LangChain + LangGraph）。当前开发机网络环境无法安装
# （镜像对未缓存包返回 404），M1-2 使用等价状态机实现，结构可平替；环境允许时启用：
# langgraph-core>=0.2,<0.3
# langchain-core>=0.3,<0.4
# langchain-openai>=0.3,<0.4
